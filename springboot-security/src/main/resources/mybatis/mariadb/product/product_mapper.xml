<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shm.product">	
 
 	<insert id="insert" parameterType="com.shm.product.ProductEntity" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO PRODUCT (
			USER_EMAIL
			, CATEGORY_ID
			, SOLD
			, NAME
			, PRICE
			, VIEW_COUNT
			, SELL_COUNT
			, DESCRIPTION
			, THUMBNAIL_URL
			, FRST_REG_DATE
			, FRST_REG_USER_ID
			, LAST_CHG_DATE
			, LAST_CHG_USER_ID
		) VALUES (
			#{userEmail}
			, #{categoryId}
			, #{sold}
			, #{name}
			, #{price}
			, #{viewCount}
			, #{description}
			, #{sellCount}
			, #{thumbnailUrl}
			<choose>
				<when test="frstRegDate != null">, #{frstRegDate}</when>
				<otherwise>, current_timestamp()</otherwise>
			</choose>
			<choose>
				<when test="frstRegUserId != null">, #{frstRegUserId}</when>
				<otherwise>, 'SYSTEM'</otherwise>
			</choose>
			<choose>
				<when test="lastChgDate != null">, #{lastChgDate}</when>
				<otherwise>, current_timestamp()</otherwise>
			</choose>
			<choose>
				<when test="lastChgUserId != null">, #{lastChgUserId}</when>
				<otherwise>, 'SYSTEM'</otherwise>
			</choose>
		)
	</insert>

	<!-- 
	<select id="select" resultType="com.shm.product.ProductEntity">
		SELECT
			ID
			, USER_EMAIL
			, CATEGORY_ID
			, SOLD
			, NAME
			, PRICE
			, VIEW_COUNT
			, SELL_COUNT
			, DESCRIPTION
			, THUMBNAIL_URL
			, FRST_REG_DATE
			, FRST_REG_USER_ID
			, LAST_CHG_DATE
			, LAST_CHG_USER_ID
		FROM PRODUCT
		ORDER BY LAST_CHG_DATE DESC
	</select>
	 -->
	
	<select id="selectById" parameterType="int" resultType="com.shm.product.ProductEntity">
		SELECT
			ID
			, USER_EMAIL
			, CATEGORY_ID
			, SOLD
			, NAME
			, PRICE
			, VIEW_COUNT
			, SELL_COUNT
			, DESCRIPTION
			, THUMBNAIL_URL
			, FRST_REG_DATE
			, FRST_REG_USER_ID
			, LAST_CHG_DATE
			, LAST_CHG_USER_ID
		FROM CATEGORY
		WHERE ID = #{id}
	</select>
	
	<select id="selectByUserEmail" parameterType="int" resultType="com.shm.product.ProductEntity">
		SELECT
			ID
			, USER_EMAIL
			, CATEGORY_ID
			, SOLD
			, NAME
			, PRICE
			, VIEW_COUNT
			, SELL_COUNT
			, DESCRIPTION
			, THUMBNAIL_URL
			, FRST_REG_DATE
			, FRST_REG_USER_ID
			, LAST_CHG_DATE
			, LAST_CHG_USER_ID
		FROM CATEGORY
		WHERE USER_EMAIL = #{userEmail}
		ORDER BY LAST_CHG_DATE DESC
	</select>
	
</mapper>
